import 'package:flutter/material.dart';

class IncomeListScreen extends StatelessWidget {
  const IncomeListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('收入列表'),
      ),
      body: const Center(
        child: Text('收入列表功能開發中...'),
      ),
    );
  }
}